/*
package CS2050Reschke.M02.Week4;

public class CsvValidation {

    public static void main(String[] args) {
        // 1) Validate structure (must have exactly 4 fields)
        String[] parts = line.split(",");
        if (parts.length != 4) {
            System.out.println("Line " + lineNumber + ": wrong number of fields (need 4) → " + line);
            return null;
        }
        // 2) Trim each field to eliminate extra white space
        String title = parts[0].trim();

        Validate Required Text Field
        // 3) Validate required text fields
        if (title.isEmpty()) {
            System.out.println("Line " + lineNumber + ": missing title");
            return null;
        }

    }
}
*/
